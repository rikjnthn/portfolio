export interface LinkType {
  path: string;
  value: string;
};

export interface ProjectType {
  href: string;
  imgPath: string;
  title: string;
  description: string;
}

export interface IconComponentsType {
  iconPath?: string;
  href: string;
  alt: string;
}