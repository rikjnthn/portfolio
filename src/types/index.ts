export interface LinkType {
  path: string;
  value: string;
};

export interface ProjectImageType {
  path: string;
  imgPath: string;
  alt: string;
}

export interface IconComponentsType {
  iconPath?: string;
  href: string;
  alt: string;
}