export interface LinkType {
  path: string;
  value: string;
};

export interface ProjectType {
  path: string;
  imgPath: string;
  alt: string;
  title: string;
  description: string;
}

export interface IconComponentsType {
  iconPath?: string;
  href: string;
  alt: string;
}